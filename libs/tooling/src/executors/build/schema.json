{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "cli": "nx",
  "title": "Build executor",
  "description": "Creates a packaged extension for browsers",
  "type": "object",
  "properties": {
    "outputPath": {
      "type": "string",
      "description": "The output path for the built extension"
    },
    "target": {
      "type": "string",
      "enum": ["firefox", "chrome"],
      "description": "The target browser to build the extension for"
    },
    "name": {
      "type": "string",
      "description": "The name of the extension"
    },
    "description": {
      "type": "string",
      "description": "The description for the extension"
    },
    "popup": {
      "type": "object",
      "properties": {
        "main": {
          "type": "string",
          "description": "File path for a popup script"
        },
        "tsConfig": {
          "type": "string",
          "description": "File path for a popup script"
        },
        "styles": {
          "type": "string",
          "description": "File path for a popup styles"
        }
      }
    },
    "background": {
      "type": "object",
      "properties": {
        "main": {
          "type": "string",
          "description": "File path for a background script"
        },
        "tsConfig": {
          "type": "string",
          "description": "File path for a popup script"
        }
      }
    }
  },
  "required": ["target", "outputPath", "name", "description"]
}
