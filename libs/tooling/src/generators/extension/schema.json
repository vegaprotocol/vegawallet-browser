{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "Extension",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "description": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "directory": {
      "type": "string",
      "description": "A directory where the project is placed"
    },
    "target": {
      "type": "string",
      "enum": ["firefox", "chrome"],
      "description": "The target browser to build the extension for"
    },
    "popupHtml": {
      "type": "string",
      "description": "File path for a popup html page"
    },
    "popupJs": {
      "type": "string",
      "description": "File path for a popup script"
    },
    "popupStyles": {
      "type": "string",
      "description": "File path for a popup script"
    },
    "popupTitle": {
      "type": "string",
      "description": "Title for the popup app"
    },
    "popupDescription": {
      "type": "string",
      "description": "Description for the popup app"
    },
    "backgroundJs": {
      "type": "string",
      "description": "File path for a background script"
    },
    "contentJs": {
      "type": "string",
      "description": "File path for a content script"
    }
  },
  "required": ["name", "directory"]
}
